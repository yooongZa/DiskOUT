# macOS
.DS_Store

# Xcode build artifacts
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.app
*.dSYM

# Xcode user state
*.xcent

# /tmp build path used in README
DiskOUT-derived/
EjectDrives-derived/

# macOS sync conflict duplicates (예: "DiskOUT 2.xcodeproj", "DiskOUT 3.xcodeproj" 등)
* [0-9].xcodeproj/
* [0-9]/

# 폐기된 sandbox/helper 실험 코드 — 빌드 미사용. 보존만 (필요 시 archive/ 안에서 복원)
archive/

# 내부 개발 문서 (공개 저장소 미포함 — DiskOUT_분석.md / _개발기획서.md / _애니메이션_가이드.md 등)
# CHANGELOG.md, README.md 는 prefix 가 다르므로 영향 없음
DiskOUT_*.md
